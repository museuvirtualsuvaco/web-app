import styles from "./Sobre.module.css"

const Sobre = () => {
    
    return (
        <div className={styles.mainContainer}>
            Componente "Sobre"
        </div>            
    );
};

export default Sobre;